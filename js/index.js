let mainArray = document.getElementById("mainArray");
// if (mainArray.innerHTML) { } else {
//     mainArray.classList.remove("bg-dark")
// }
mainArray.innerText ? {} : mainArray.classList.remove("bg-dark")